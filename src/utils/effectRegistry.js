// Registry de efeitos e campos (placeholders)
export const effectCards = {
  // 'heal-small': { name: 'Cura Pequena', play: (ctx) => {} },
};

export const fieldCards = {
  // 'purple-field': { name: 'Campo Púrpura', onEnter: (ctx) => {}, onExit: (ctx) => {} },
};
